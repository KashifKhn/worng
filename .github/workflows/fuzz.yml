name: Fuzz

on:
  # Run every night at 02:00 UTC
  schedule:
    - cron: "0 2 * * *"
  # Allow manual trigger with configurable fuzz time
  workflow_dispatch:
    inputs:
      fuzztime:
        description: "Fuzz duration per target (e.g. 30m, 1h, 2h)"
        required: false
        default: "1h"

permissions:
  contents: read
  # Needed to open issues on crash findings
  issues: write

concurrency:
  group: fuzz
  cancel-in-progress: false

jobs:
  fuzz:
    name: Fuzz ${{ matrix.target }}
    runs-on: ubuntu-latest
    # Hard stop so a wedged target cannot burn runner hours past
    # fuzztime + minimization overhead.
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: FuzzLexer
            package: ./internal/lexer/...
            corpus: internal/lexer/testdata/fuzz/FuzzLexer/
          - target: FuzzParser
            package: ./internal/parser/...
            corpus: internal/parser/testdata/fuzz/FuzzParser/
          - target: FuzzInterpreter
            package: ./internal/interpreter/...
            corpus: internal/interpreter/testdata/fuzz/FuzzInterpreter/

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: true

      - name: Fuzz ${{ matrix.target }}
        run: |
          # Scheduled runs have no inputs, so this fallback IS the nightly
          # duration — 1h per target balances coverage against runner cost.
          FUZZTIME="${{ github.event.inputs.fuzztime || '1h' }}"
          go test ${{ matrix.package }} \
            -fuzz=${{ matrix.target }} \
            -fuzztime=${FUZZTIME} \
            -fuzzminimizetime=30s

      - name: Upload crash corpus on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: fuzz-crash-${{ matrix.target }}
          path: |
            ${{ matrix.corpus }}
          if-no-files-found: ignore

      - name: Open issue on crash
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const title = `Fuzz crash detected: ${{ matrix.target }}`;
            const body = [
              `The nightly fuzz run found a crash in \`${{ matrix.target }}\`.`,
              ``,
              `**Run:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}`,
              ``,
              `Download the crash corpus artifact \`fuzz-crash-${{ matrix.target }}\` from the run above and reproduce locally:`,
              `\`\`\``,
              `go test ${{ matrix.package }} -run=${{ matrix.target }} -v`,
              `\`\`\``,
            ].join('\n');

            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'fuzz-crash',
            });

            const alreadyOpen = issues.some(i => i.title === title);
            if (!alreadyOpen) {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
                labels: ['fuzz-crash', 'bug'],
              });
            }
